import React from 'react';
import { connect } from 'dva';

function IndexPage() {
  return (
    <div>
      rent313
    </div>
  );
}

IndexPage.propTypes = {
};

export default connect()(IndexPage);
